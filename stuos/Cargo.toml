[package]
name = "stuos"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[build]
target = "TARGET"

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"

[dependencies]
